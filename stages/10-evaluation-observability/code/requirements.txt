# Local demo, regression and checks use only the Python standard library.
# Optional: DeepSeek-compatible client and local OpenTelemetry SDK export.
openai>=2,<3
opentelemetry-api>=1.30,<2
opentelemetry-sdk>=1.30,<2
