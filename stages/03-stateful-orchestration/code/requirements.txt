# workflow.py and state_graph.py use only the standard library.
# LangGraph examples require the framework; only the live entry needs OpenAI.
langgraph>=1.0,<2
openai>=2,<3
typing_extensions>=4.12,<5
