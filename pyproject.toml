[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "tiny-agent-runtime"
version = "0.1.0"
description = "A learning-first, production-minded agent runtime built from scratch."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
openai = ["openai>=2,<3"]
stage03 = ["langgraph>=1.2,<2", "langchain>=1.3,<2"]
stage04 = ["numpy>=1.26", "faiss-cpu>=1.9,<2", "qdrant-client>=1.14,<2", "langchain>=1.3,<2"]
stage05 = ["mcp[cli]>=2,<3"]
stage06 = ["langgraph>=1.2,<2", "langgraph-checkpoint-sqlite>=3.1,<4", "langgraph-checkpoint-postgres>=3.1,<4", "psycopg[binary,pool]>=3.3,<4"]
stage07 = ["jsonschema>=4.25,<5", "tenacity>=9,<10", "pydantic>=2.11,<3"]
stage08 = ["langsmith>=0.11,<1", "opentelemetry-api>=1.42,<2", "opentelemetry-sdk>=1.42,<2"]
stage09 = ["openai-agents>=0.22,<1", "a2a-sdk>=1.1,<2"]
stage10 = [
    "fastapi>=0.141,<1",
    "uvicorn[standard]>=0.52,<1",
    "pydantic-settings>=2.15,<3",
    "redis>=8.1,<9",
    "psycopg[binary,pool]>=3.3,<4",
    "httpx>=0.28,<1",
    "a2a-sdk>=1.1,<2",
]
dev = ["pytest>=8.0"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
